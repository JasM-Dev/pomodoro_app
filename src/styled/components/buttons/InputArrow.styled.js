import styled from "styled-components";

const StyledInputArrows = styled.button`
  display: block;
  background: transparent;
  height: 100%;
  svg {
    fill: none;
    stroke: #1e213f;
    stroke-opacity: 0.25;
    stroke-width: 2;
  }
`;

export default StyledInputArrows;
